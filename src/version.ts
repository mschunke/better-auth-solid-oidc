/**
 * Reported as the plugin's `version` so a Better Auth instance can identify
 * which build of this plugin is loaded.
 *
 * Kept in sync with `package.json` by the release script.
 */
export const PACKAGE_VERSION = "0.1.0";
